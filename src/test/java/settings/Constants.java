package settings;

import java.nio.file.Paths;

public class Constants {
    public final static String RESOURCE_PATH = Paths.get(Paths.get("").toString(), "src", "test", "resources").toString();
    public final static String ROOT_PATH = Paths.get(Paths.get("").toString(), "src", "test", "resources").toString();
}
